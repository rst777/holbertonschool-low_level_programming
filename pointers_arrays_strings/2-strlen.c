#include "main.h"

/**
 * _strlen - Calcule la longueur d'une chaîne de caractères.
 * @s: La chaîne de caractères dont la longueur est calculée.
 *
 * Return: La longueur de la chaîne (nombre de caractères).
 */

int _strlen(char *s)
{
	int i;



	for (i = 0; s[i] != '\0'; i++)
	{

	}
		return (i);

}
