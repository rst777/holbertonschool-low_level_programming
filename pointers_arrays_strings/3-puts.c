#include"main.h"
#include <stdio.h>
/**
 * _puts - Affiche une chaîne de caractères suivie d'un saut de ligne
 * @str: La chaîne de caractères à afficher
 *
 * Description: Cette fonction utilise la fonction `puts` pour afficher
 * la chaîne de caractères passée en argnt, suivie d'un saut de ligne.
 */

void _puts(char *str)

{
	puts(str);
}
