#include"main.h"
#include <stdlib.h>
/*
 * print_last_digit Computes the last digit of a given number.
 *
 * num - The number whose last digit is to be computed.
 *
 * Returns:
 */
int print_last_digit(int num)

{
	return (abs(num % 10));
}
